function safeParse(text) {
    // replace bare NaN tokens (not inside strings) with 0
    const cleaned = text.replace(/:\s*NaN\b/g, ': 0')
                        .replace(/\bNaN\b/g, '0');  // also catches NaN inside arrays
    return JSON.parse(cleaned);
}

const data = safeParse(rawText);
