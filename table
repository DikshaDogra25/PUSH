out = convert_to_table(data)                 # this is already the correct raw string
with open("test.html", "w", encoding="utf-8") as f:
    f.write(out)                             # NOT json.dumps(out), NOT print(repr(out))
