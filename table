# collect all product values from the 2nd list
wanted_products = {row["product"] for row in second_list if "product" in row}

# keep purchase deals whose Product is in that set
filtered = [deal for deal in purchase_deals if deal.get("Product") in wanted_products]
