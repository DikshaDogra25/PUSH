const group = FINAL_PLAN.find(p => p.tag_index === tag.id);
const linkedPurchases = (group?.rows || [])
  .filter(r => r.type === "PURCHASE")
  .map(r => String(r.deal_number))   // deal_number is numeric — coerce before join
  .join(", ");
// e.g. "2548274, 2548301"  (empty string "" if none)
